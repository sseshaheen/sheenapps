/**
 * Generate Pre-purged CSS for Templates
 * 
 * Build-time script that extracts Tailwind classes from template components
 * and generates optimized CSS files for each template variant.
 * 
 * NOTE: This is now a stub as the preview system has been simplified.
 * Templates will use Tailwind CDN or their own bundled CSS.
 */

console.log('⚡ Skipping CSS generation - templates now handle their own styling')
process.exit(0)